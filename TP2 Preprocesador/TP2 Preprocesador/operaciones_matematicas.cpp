#include "stdafx.h"
#include "operaciones_matematicas.h"
#define PI 3.14
int square(int n) 
{
	return ((n)*(n));
}

int sum(int a, int b) 
{
	return (a) + (b);
}

double area(double r) 
{
	return (PI*((r)*(r)));
}

int max(int a, int b) 
{
	return ((a) > (b) ? (a) : (b));
}

